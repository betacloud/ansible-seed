---
ansible_roles_path: galaxy

# directories

configuration_directory: /opt/configuration
secrets_directory: /opt/secrets
venv_directory: /opt/venv-ansible

kolla_private_key: |
  -----BEGIN RSA PRIVATE KEY-----
  ...
  -----END RSA PRIVATE KEY-----

# parameters for nexus

nexus_password: password
nexus_roles_repository: ansible
nexus_server: nexus.betacloud.io
nexus_username: customer

nexus_roles:
  - name: betacloud.bootstrap
    version: latest
  - name: betacloud.kolla
    version: latest
  - name: betacloud.seed
    version: latest
  - name: betacloud.ufw
    version: latest
  - name: betacloud.xenserver
    version: latest
